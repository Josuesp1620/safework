/**
 * What the platform and each customer say a work permit needs.
 *
 * Sites, areas, kinds of work and the controls each kind demands. Everything
 * here is versioned, because a permit approved in February has to keep showing
 * February's list of controls even after somebody edits it in March — a change
 * in configuration must never rewrite what an inspector reads.
 */
package com.dooservice.safework.catalog;
